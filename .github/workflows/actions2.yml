name : This is a github action
on: [push]
jobs: 
      Echo:
            runs-on: ubuntu-latest
            steps:
            - name: checkout repo
              uses: actions/checkout@v4
            - name: echo hello 
              run: echo hello
            - name: cat temp.txt
              run: cat temp.txt
